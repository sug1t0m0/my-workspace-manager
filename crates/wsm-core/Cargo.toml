# Rust 版 wsm-core。zsh 版 (bin/wsm-core) と JSON API 互換で、完成まで並存する。
# ビルド成果物は target/debug/wsm-core に出るため bin/ とは衝突しない。
[package]
name = "wsm-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
serde_json = "1"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
