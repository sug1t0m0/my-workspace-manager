# wsm-server (JSON API)。仕様は docs/wsm.md、挙動は契約テストが固定する。
[package]
name = "wsm-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
serde_json = "1"
wsm-shared = { path = "../shared" }

[dev-dependencies]
serde_json = "1"
tempfile = "3"
